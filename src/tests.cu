# include "matrix_transpose.cuh"
#include "tests.cuh"
#include "main.cuh"
void test_matrix_transpose(){
	std::cout << "Testing matrix transpose" << std::endl;

	int **in = generateRandomMatrix(100, 100);
}